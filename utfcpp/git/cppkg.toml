[package]
name = "utfcpp"
# if you use git repo, package version is git
version = "git"
description = "cpp utf8 library"
global=false
# library name in cmake, cppm.toml use this value 
# [dependencies]
#${library_name} = {module =${value}}
cmake = {name = "utf8::cpp", findlib=""}
# write download value git or url
download = {git="https://github.com/nemtrif/utfcpp.git"}
#download = {url=""}
